#logging:
#    level:
  #    ROOT: DEBUG
#    ca.uhn.fhir.rest.server: DEBUG

server:
    port: 9085
    use-forward-headers: true
    session:
        cookie:
            http-only: true
    contextPath: /
    
spring:
    application:
        name: oda-epidemic-service
    profiles:
        active: dev
