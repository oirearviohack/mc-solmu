#logging:
#    level:
  #    ROOT: DEBUG
#    ca.uhn.fhir.rest.server: DEBUG

server:
    port: 6086
    use-forward-headers: true
    session:
        cookie:
            http-only: true
    contextPath: /
    
spring:
    application:
        name: oda-dss-service
    profiles:
        active: dev
